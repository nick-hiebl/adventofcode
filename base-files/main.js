const { toInt } = require("../utils");

const print = (...arguments) => {
  console.log(...arguments);
};

let example = ``.split('\n').filter(t => !!t);

const real = ``.split('\n').filter(t => !!t);

const lines = example;

let total = 0;

for (const line of lines) {
  
}

print(total);
